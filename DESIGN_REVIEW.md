# Team 设置界面设计评审

- 评审日期：2026-08-23（信息架构于 2026-08-25 修订）
- 评审对象：`docs/prototypes/team-workspace-demo.html`
- 产品依据：`docs/acceptance/team-owner-member-product-spec.zh-CN.md`
- 范围：Team 设置的信息架构、Owner / 成员权限表达、用量、成员、邀请码、个人账号共享的产品边界与 Team 头部的成员关系操作；不把原型 fixture 当作真实账单或线上状态。

## 结论

当前 Demo 已将一级导航收敛为`用量 / 成员 / 邀请码`三个任务；个人账号共享沿用现有 Codex 账号管理入口，Team 设置不再重复展示`我的共享`模块，当前身份与成员关系操作收口到 Team 头部。用量以预估费用（USD）、Token 用量和请求次数呈现，不再展示 Credits、虚构余额或解释性文案块；成员页使用`邀请成员`表达意图，进入流程后才`生成邀请码`；邀请码关闭后仍保留安全的“再次查看”入口。

这不等于生产功能已经全部交付。Host 安全信封、短时 CSRF capability、全量并发协议与真实价格合同仍以产品 Spec 和后端验收为准。

## 截图证据

> 注：下列截图记录于 2026-08-23，早于 2026-08-25 的导航收敛修订，只作为原有用量和邀请流程的历史证据，不代表最新导航的视觉验收结果。

| 场景 | 视口 | 文件 | 检查结果 |
| --- | ---: | --- | --- |
| Owner / 用量 | 1280 × 900 | `screenshots/final-team-owner-desktop-1280.png` | 双栏汇总层级清晰；主导航与内容不重叠 |
| Owner / 用量 | 768 × 1024 | `screenshots/final-team-owner-tablet-768.png` | Team 上下文先于导航；双栏指标仍可比较 |
| Owner / 用量 | 375 × 812 | `screenshots/final-team-owner-mobile-375.png` | 单栏堆叠；实际 `innerWidth = scrollWidth = 375`，无页面级横向溢出 |
| Owner / 邀请码 | 1280 × 900 | `screenshots/final-team-invites-desktop-1280.png` | 新邀请码可查看；旧摘要邀请码明确提示撤销重建 |
| Owner / 再次查看邀请码 | 1280 × 900 | `screenshots/final-team-invite-reveal-desktop-1280.png` | 明文只在模态框出现；背景锁定且操作层级明确 |

浏览器运行检查：三种用量视口均无 Runtime 异常、console warning/error 或横向溢出。邀请码交互实测为：首次查看成功 → 关闭后明文从 DOM 清除且解除背景锁定 → 列表“查看邀请码”仍存在 → 再次查看成功。

2026-08-25 信息架构修订后又完成一次交互浏览器复核：桌面 Owner 成员页以“邀请成员”进入邀请码页，实际创建动作才显示“生成邀请码”；普通成员不显示邀请码导航，Team 菜单只显示“退出 Team”；375 × 812 下 `innerWidth = scrollWidth = 375`，无横向溢出，console 无 warning/error。此次复核未另存截图文件，下表旧截图仍只作历史证据。

## 已修复的评审问题

1. **信息架构**：Team 标题、状态与当前角色成为稳定上下文；左侧只保留用量、成员和 Owner 邀请码，个人账号共享沿用现有账号管理入口，Team 设置不重复展示，退出/转让/解散按角色收口到 Team 菜单。
2. **用量语义**：主指标改为预估费用（USD），Token 与请求次数作为辅助指标；完整数据作为默认正常态不再额外贴标签，只有部分、价格未知、未计量等例外状态使用短标签表达。
3. **角色表达**：只出现 Team Owner 和成员，不显示头像、Admin、邮箱、在线状态或虚构活跃信息。
4. **邀请码生命周期**：列表不展示明文；当前 Owner 可再次查看可恢复的新邀请码；旧版仅有摘要的有效码不能伪造恢复。
5. **加入防误操作**：无效或失效邀请码不会进入预览与确认加入；确认时再次检查有效状态。
6. **成员名称**：原型覆盖 trim、NFKC、1–120 Unicode 标量、控制/不可见字符和规范化重名；精确 Unicode 15.1 NFKC_Casefold 仍归服务端。
7. **所有权转让**：只列出拥有有效 Team 设备凭据的成员；请求明确 24 小时到期，接受前 Owner 身份不变。
8. **响应式**：768px 导航顺序修正；375px 使用真实设备度量验证；主要按钮至少 44px，弹窗打开时锁定背景滚动。

## 设计质量检查

- **视觉层级**：Team 名称 > 当前任务 > 核心指标 > 次级状态，金额最醒目，符合用户先判断消费规模的路径。
- **一致性**：主操作、次操作、危险操作、状态标签和列表行使用统一样式；解散、撤销、移出保持危险色且不与普通保存混淆。
- **可读性**：界面不再依赖长解释段落；中文标签直接说明对象和单位。窄屏只改变布局，不改变任务顺序或信息语义。
- **可访问性**：导航使用 `aria-current`，菜单与弹窗声明相应语义；关闭按钮有可访问名称；弹窗支持焦点回退，并在关闭时清除敏感明文。
- **原型边界**：底部“原型场景”仅用于状态验收，不进入正式产品信息架构；所有金额、Token、姓名和邀请码均为 fixture。

## 2026-08-26「团队设置」点击后重做复核

本轮实现保留原有 Team 落地页，只有点击既有的“团队设置”按钮后才进入管理工作台；工作台默认打开“账号”页。Owner 自有账号卡片增加近 7 天请求、Token、预估费用与请求明细，并可在编辑保护弹窗中设置每周共享金额上限。金额只约束其他成员使用该账号产生的已定价请求，Owner 自用不计入共享上限。

自动化组件验收覆盖了以下关键路径：点击前工作台内容不在 DOM；点击后默认进入账号页；账号卡显示“本周约 $已用 / $上限”；编辑弹窗可保存每周上限；近期请求弹窗不暴露消费成员身份；取消请求会释放入场预留金额。

真实 DSH 运行时复核使用本轮 tarball 和 `@deepseek-ai/dsh@0.1.0-rc.8`。当前可用的测试 Team 后端仍返回旧协议，缺少必需的 `lifecycleRevision`，因此实际运行只能到达安全错误态，不能把下列截图表述为工作台完成态。三种视口均确认无页面级横向溢出：

| 场景 | 视口 | 文件 | 检查结果 |
| --- | ---: | --- | --- |
| 旧协议错误态 | 1280 × 800 | `screenshots/team-settings-rework-runtime-desktop.png` | `innerWidth = scrollWidth = 1280` |
| 旧协议错误态 | 768 × 1024 | `screenshots/team-settings-rework-runtime-tablet.png` | `innerWidth = scrollWidth = 768` |
| 旧协议错误态 | 375 × 812 | `screenshots/team-settings-rework-runtime-mobile.png` | `innerWidth = scrollWidth = 375` |

未完成的视觉证据：连接当前协议 Team 后端后的桌面、平板、手机工作台完成态截图。当前完成态的行为证据来自 jsdom 组件测试，不等同于真实浏览器视觉验收。

## 正式实现前仍需验证

- 用真实 DSH 字体栈、主题变量和设置容器复核一次视觉差异。
- 为 2026-08-25 收敛后的三入口导航、移除重复共享区后的用量页和成员 Team 菜单补存新的宽/窄屏截图，作为已完成交互浏览器验收的持久证据。
- 以键盘完整走查导航、菜单、弹窗、危险确认和焦点回退，并用自动化工具补充对比度检查。
- 生产 Browser 不得持有原始 Team Key；邀请码再次查看必须由 Host 安全信封、`no-store` 响应和最长 60 秒显示窗口实现。
- 真实用量接口必须分别提供 Token 覆盖和费用覆盖，不能由 Browser 猜测或把内部 Credits 映射成金额。
