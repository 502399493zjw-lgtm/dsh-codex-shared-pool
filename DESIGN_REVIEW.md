# Team 设置界面设计评审

- 评审日期：2026-08-23（信息架构于 2026-08-25 修订）
- 评审对象：`docs/prototypes/team-workspace-demo.html`
- 产品依据：`docs/acceptance/team-owner-member-product-spec.zh-CN.md`
- 范围：Team 设置的信息架构、Owner / 成员权限表达、用量、成员、邀请码、个人账号共享的产品边界与 Team 头部的成员关系操作；不把原型 fixture 当作真实账单或线上状态。

## 结论

当前 Demo 已将一级导航收敛为`用量 / 成员 / 邀请码`三个任务；个人账号共享沿用现有 Codex 账号管理入口，Team 设置不再重复展示`我的共享`模块，当前身份与成员关系操作收口到 Team 头部。用量以预估费用（USD）、Token 用量和请求次数呈现，不再展示 Credits、虚构余额或解释性文案块；成员页使用`邀请成员`表达意图，进入流程后才`生成邀请码`；邀请码关闭后仍保留安全的“再次查看”入口。

这不等于生产功能已经全部交付。Host 安全信封、短时 CSRF capability、全量并发协议与真实价格合同仍以产品 Spec 和后端验收为准。

## 截图证据

> 注：下列截图记录于 2026-08-23，早于 2026-08-25 的导航收敛修订，只作为原有用量和邀请流程的历史证据，不代表最新导航的视觉验收结果。

| 场景 | 视口 | 文件 | 检查结果 |
| --- | ---: | --- | --- |
| Owner / 用量 | 1280 × 900 | `screenshots/final-team-owner-desktop-1280.png` | 双栏汇总层级清晰；主导航与内容不重叠 |
| Owner / 用量 | 768 × 1024 | `screenshots/final-team-owner-tablet-768.png` | Team 上下文先于导航；双栏指标仍可比较 |
| Owner / 用量 | 375 × 812 | `screenshots/final-team-owner-mobile-375.png` | 单栏堆叠；实际 `innerWidth = scrollWidth = 375`，无页面级横向溢出 |
| Owner / 邀请码 | 1280 × 900 | `screenshots/final-team-invites-desktop-1280.png` | 新邀请码可查看；旧摘要邀请码明确提示撤销重建 |
| Owner / 再次查看邀请码 | 1280 × 900 | `screenshots/final-team-invite-reveal-desktop-1280.png` | 明文只在模态框出现；背景锁定且操作层级明确 |

浏览器运行检查：三种用量视口均无 Runtime 异常、console warning/error 或横向溢出。邀请码交互实测为：首次查看成功 → 关闭后明文从 DOM 清除且解除背景锁定 → 列表“查看邀请码”仍存在 → 再次查看成功。

2026-08-25 信息架构修订后又完成一次交互浏览器复核：桌面 Owner 成员页以“邀请成员”进入邀请码页，实际创建动作才显示“生成邀请码”；普通成员不显示邀请码导航，Team 菜单只显示“退出 Team”；375 × 812 下 `innerWidth = scrollWidth = 375`，无横向溢出，console 无 warning/error。此次复核未另存截图文件，下表旧截图仍只作历史证据。

## 已修复的评审问题

1. **信息架构**：Team 标题、状态与当前角色成为稳定上下文；左侧只保留用量、成员和 Owner 邀请码，个人账号共享沿用现有账号管理入口，Team 设置不重复展示，退出/转让/解散按角色收口到 Team 菜单。
2. **用量语义**：主指标改为预估费用（USD），Token 与请求次数作为辅助指标；完整数据作为默认正常态不再额外贴标签，只有部分、价格未知、未计量等例外状态使用短标签表达。
3. **角色表达**：只出现 Team Owner 和成员，不显示头像、Admin、邮箱、在线状态或虚构活跃信息。
4. **邀请码生命周期**：列表不展示明文；当前 Owner 可再次查看可恢复的新邀请码；旧版仅有摘要的有效码不能伪造恢复。
5. **加入防误操作**：无效或失效邀请码不会进入预览与确认加入；确认时再次检查有效状态。
6. **成员名称**：原型覆盖 trim、NFKC、1–120 Unicode 标量、控制/不可见字符和规范化重名；精确 Unicode 15.1 NFKC_Casefold 仍归服务端。
7. **所有权转让**：只列出拥有有效 Team 设备凭据的成员；请求明确 24 小时到期，接受前 Owner 身份不变。
8. **响应式**：768px 导航顺序修正；375px 使用真实设备度量验证；主要按钮至少 44px，弹窗打开时锁定背景滚动。

## 设计质量检查

- **视觉层级**：Team 名称 > 当前任务 > 核心指标 > 次级状态，金额最醒目，符合用户先判断消费规模的路径。
- **一致性**：主操作、次操作、危险操作、状态标签和列表行使用统一样式；解散、撤销、移出保持危险色且不与普通保存混淆。
- **可读性**：界面不再依赖长解释段落；中文标签直接说明对象和单位。窄屏只改变布局，不改变任务顺序或信息语义。
- **可访问性**：导航使用 `aria-current`，菜单与弹窗声明相应语义；关闭按钮有可访问名称；弹窗支持焦点回退，并在关闭时清除敏感明文。
- **原型边界**：底部“原型场景”仅用于状态验收，不进入正式产品信息架构；所有金额、Token、姓名和邀请码均为 fixture。

## 2026-08-26「团队设置」点击后重做复核

本轮最终信息架构把 Team 账号面板恢复为“团队”标签的默认内容：Team 上下文、添加账号、Owner 自有账号卡片、近 7 天请求、Token、预估费用与保护设置都留在面板中。只有点击“团队设置”后才进入管理工作台，且工作台只保留“用量 / 成员 / 邀请码”和 Team 生命周期操作，不再重复账号入口。每周共享金额上限只约束其他成员使用该账号产生的已定价请求，Owner 自用不计入共享上限。

自动化组件验收覆盖了以下关键路径：默认 Team 面板内存在账号区且管理工作台不在 DOM；点击后默认进入用量页且账号区退出 DOM；返回后焦点恢复到“团队设置”；账号卡显示“本周约 $已用 / $上限”；编辑弹窗可保存每周上限；近期请求弹窗不暴露消费成员身份；取消请求会释放入场预留金额。

真实 DSH 运行时复核最终使用 tarball（SHA-256 `07b70577c4c3f4637b747c77d02feb094098d538f49431e607ab00908966740c`）、`@deepseek-ai/dsh@0.1.0-rc.8` 和隔离的内存型 Team Host / Client。测试后端使用当前协议，Team overview 返回 `lifecycleRevision: 1`。浏览器实测确认：点击前存在“团队面板”和“账号”区域而“团队设置”工作台不在 DOM；点击后出现带 `SETTINGS / 团队设置` 导轨的“用量 / 成员 / 邀请码”三入口并默认打开用量，账号区域退出 DOM；成员页和邀请码页也可正常切换。隔离 Team 没有贡献账号，因此本次不把账号编辑、每周上限或请求明细弹窗标记为真实后端已验证。浏览器 console 无 warning / error：

| 场景 | 视口 | 文件 | 检查结果 |
| --- | ---: | --- | --- |
| 默认 Team 面板 / 账号空态 | 1280 × 800 | `screenshots/team-panel-runtime-desktop.png` | 点击入口与账号区清晰，管理工作台尚未出现 |
| 点击后管理工作台 / 用量零态 | 1440 × 900 | `screenshots/team-settings-rework-runtime-desktop.png` | 左侧显示“团队设置”，返回入口位于右侧 Team 内容区顶部；费用优先的双栏数据层级清晰 |
| 点击后管理工作台 / 用量零态 | 768 × 1024 | `screenshots/team-settings-rework-runtime-tablet.png` | 工作台仍在 DOM，但宿主主界面层级盖住设置弹窗，不能标记为可用性通过 |
| 点击后管理工作台 / 用量零态 | 375 × 812 | `screenshots/team-settings-rework-runtime-mobile.png` | 插件已切换极窄容器降级布局，但宿主固定左侧导航只留下约 140px，不能标记为手机可用性通过 |

本轮真实运行证据覆盖当前协议连接、默认账号空态、点击进入与返回、用量零态和三档响应式度量。桌面的插件布局通过视觉复核；768px 下宿主主界面层级会盖住仍在 DOM 的设置弹窗，375px 下设置对话框的左侧产品导航仍占固定宽度，导致右侧插件内容区过窄。本仓库不能越过 Cordis 插件边界修改 DSH 核心布局。尚未完成的真实运行证据是：至少贡献一个账号后，对账号卡、每周共享上限编辑、近期请求明细及取消请求释放预留金额的端到端验收；这些路径目前仍由组件和协议测试覆盖。

## 后续仍需验证

- 在 stock DSH 修复设置壳的平板层叠和手机固定侧栏布局后，重新完成 768px / 375px 可用性验收；当前截图只证明插件未造成页面级横向溢出。
- 使用至少一个真实贡献账号，端到端验证账号卡、每周共享上限编辑、近期请求明细及取消请求释放预留金额。
- 以键盘完整走查导航、菜单、弹窗、危险确认和焦点回退，并用自动化工具补充对比度检查。
- 生产 Browser 不得持有原始 Team Key；邀请码再次查看必须由 Host 安全信封、`no-store` 响应和最长 60 秒显示窗口实现。
- 真实用量接口必须分别提供 Token 覆盖和费用覆盖，不能由 Browser 猜测或把内部 Credits 映射成金额。

## 2026-08-26 原型一致性纠偏

上一轮把 stock DSH 左侧设置导航与 Team 工作台内部导航判断为“重复”，因此移除了原型中的 `SETTINGS / Team` 导轨，并把用量改成了顶部标签与等权卡片。和已确认原型重新逐项对照后，这个方向被否决：内部导轨表达的是 Team 管理工作区，不是宿主设置分类；大号费用和横向 Token / 请求行也是原型的核心识别特征，而不是可随意替换的装饰。

本轮已恢复原型结构，同时保留已确认的产品边界：默认“团队”面板继续负责账号共享；只有点击“团队设置”才进入管理工作台；工作台只包含“用量 / 成员 / 邀请码”。具体修正如下：

- 恢复 132–152px 的紧凑内部导轨、`SETTINGS / Team` 品牌和纵向任务导航；容器低于 460px 才降级为横向导航。
- 恢复 Team 大标题和内容画布层级；用量改回共享分隔线的数据面，而不是三个等权卡片。
- 恢复“费用为主、Token 与请求次数为辅”的层级：费用使用大号数字，Token / 请求按行排列；容器低于 520px 时两组数据再堆叠。
- 保留返回焦点、`aria-current`、44px 点击目标和既有协议行为；平板下“刷新”按钮禁止换行。

桌面、平板和手机的真实 stock DSH 截图已重新生成。桌面在实际设置插槽中保留了原型的识别结构。768px 下工作台仍在 DOM，但宿主主界面层级会盖住设置弹窗，不能作为通过证据。375px 手机截图还受 stock DSH 固定设置侧栏挤压，插件获得的内容宽度约 100–140px；这两项都属于宿主布局限制，本仓库不能越过 Cordis 插件边界修改。

## 2026-08-26 结构线清晰度修复

用户复核指出桌面工作台缺少可见结构线，导轨、Team 上下文和两组用量在深色背景中揉成一块。真实截图确认根因不是没有声明边框，而是宿主的 subtle border token 与背景对比过低，1px 规则在实际渲染中接近不可见。

本轮将结构线改为由前景色混合得到的 18% / 30% 两级规则，并让线条只承担信息分区：工作区使用强外框；导轨与内容之间使用强竖线；Team 上下文与当前任务之间使用强横线；用量数据面使用完整外框、组间竖线、组标题横线和逐行横线。两组用量使用轻微独立底色，列内边距从 20px 收到 16px，并锁定标题、Token 和请求标签不换行，避免增强边界后反而挤压文字。

`screenshots/team-settings-rework-runtime-desktop.png` 已由最终 tarball 在 1440 × 900 的 stock DSH 中重拍，外框、左右分栏和数据行均可辨认。`screenshots/team-settings-rework-runtime-tablet.png` 与 `screenshots/team-settings-rework-runtime-mobile.png` 也按规范重拍；前者再次证明 768px 下宿主层级会盖住仍在 DOM 的设置弹窗，后者仍受固定设置侧栏挤压，这两项不属于插件 CSS 可修范围。

## 2026-08-26 工作区命名与返回层级修正

左侧导轨的标题用于命名当前工作区，因此由含混的 `Team` 改为明确的“团队设置”。用户进一步确认返回入口应取代导轨顶部的 `SETTINGS` 字样，因此返回键最终位于左侧导轨顶部、“团队设置”标题之前；右侧 Team 内容区不再重复返回入口。按钮使用稳定的 SVG 图标、44px 点击目标，并保留返回后焦点恢复行为。

## 2026-08-26 多角色视觉与交互复核

本轮由视觉、交互和组件体系三个只读 reviewer 分别检查用户提供的暗色运行截图、实际组件代码和当前 DSH primitives。三个 reviewer 一致确认，邀请码弹窗的原生 `<select>` 是白色系统菜单、浅色未选项和系统蓝高亮的直接原因；同时确认嵌套 Modal 的 Escape 会穿透到宿主设置层、弹窗缺少焦点约束、创建请求进行中仍可关闭，以及返回键触控尺寸不足。

本轮已完成以下修复：

1. 有效时长改为语义化 `fieldset + radio` 三段选择，1 / 7 / 30 天始终使用当前主题的边框、背景、选中态和焦点态，不再打开操作系统白色下拉层。
2. 子弹窗在捕获阶段接管 Escape，只关闭最上层；Tab / Shift+Tab 被约束在当前 dialog 内，背景 body layer 在弹窗存续期间设为 `inert`，清理时恢复原值。
3. 创建邀请码改为表单提交，支持 Enter；创建进行中禁用取消、输入和时长选择，并阻止关闭与 Escape，避免邀请已经生成但一次性凭据未展示。
4. 返回键扩大为 44×44，文本箭头替换为 SVG；弹窗操作区右对齐，字段间距增大，三段选项在窄容器下改为单列。
5. 用量区域保留工作区外框和 rail 强分隔，但卡片标题线、数据行线与双卡分隔降为普通结构线；标题允许换行，双卡在 620px 容器宽度即切为单列，降低“框里套框”和窄槽截断。

当前仍有两个边界：stock DSH 的设置壳在 768px 会被宿主层级覆盖，在 375px 固定侧栏会严重挤压插件内容，这不在插件允许修改的 Cordis 边界内；本轮浏览器截图动作又被应用的 Browser URL 安全策略拒绝，因此不能把历史截图冒充最新构建的视觉证据。发布前仍需在可用的 stock DSH 浏览器环境中重拍桌面正常态、邀请码弹窗和 1 / 7 / 30 天选择态，并验证一次 Escape 只退一层。
