# DSH rc.8 Independent Plugin Compatibility Implementation Plan

> **For Codex:** Execute this plan inline. Preserve user-owned history and data; only remove runtime/profile residue and untracked generated artifacts after recording a recoverable backup.

**Goal:** Remove obsolete core-plugin coupling and stale installation references, validate every locally owned independent plugin against stock `@deepseek-ai/dsh@0.1.0-rc.8`, then upgrade the real Web profile and restart DSH on port 3080.

**Architecture:** Keep stock DSH immutable. Each feature remains an independently packable npm package mounted through its own `cordis.patch.yml` or browser manifest. Compatibility is proven first in isolated `DSH_HOME` profiles, then in a combined profile, before the backed-up real profile is migrated.

**Tech Stack:** Node.js, pnpm, TypeScript, Vitest, Cordis patch YAML, stock `@deepseek-ai/dsh` CLI, browser DOM smoke checks.

---

### Task 1: Capture the baseline and define residue precisely

**Files:**
- Inspect: `<user-home>/.dsh/profiles/web/package.json`
- Inspect: `<user-home>/.dsh/profiles/web/cordis.patch.yml`
- Inspect: each local plugin `package.json` and `cordis.patch.yml`
- Inspect: `<user-home>/deepseek-harness/.worktrees/plugin-finalization-integration`

- [ ] Record exact Git state for every repository/worktree.
- [ ] Record the running DSH version, profile dependency sources, plugin bundles, and current `--dump-config` warnings.
- [ ] Treat tracked commits in historical branches as history, not disposable residue.
- [ ] Classify only obsolete patch entries, broken local dependency references, and untracked generated build output as removable residue.

### Task 2: Add regression checks for standalone patch ownership

**Files:**
- Modify: `<user-home>/deepseek-harness-plugins/dsh-model-retry/cordis.patch.yml`
- Test: `<user-home>/deepseek-harness-plugins/dsh-model-retry/tests/package.test.*`
- Modify: `<user-home>/deepseek-harness-plugins/dsh-session-history/cordis.patch.yml`
- Test: `<user-home>/deepseek-harness-plugins/dsh-session-history/tests/package.test.*`
- Modify: `<user-home>/dsh-codex-shared-pool/cordis.patch.yml` or profile-only override as required
- Test: relevant package verification tests

- [ ] Write failing tests asserting that each external plugin inserts only its own IDs and does not disable legacy core IDs.
- [ ] Remove `ui-settings-model-retry` and `session-history-lite` disable entries.
- [ ] Remove the real profile's obsolete `codex-quota` override.
- [ ] Run focused checks and confirm stock config composition has no `entry not found` warnings.

### Task 3: Update package compatibility metadata and rebuild

**Files:**
- Modify: each locally owned plugin `package.json` only where rc.8 evidence requires it
- Modify: lockfiles generated by the package manager where present
- Inspect/verify: build outputs and package manifests

- [ ] Compare rc.8 public package names and browser/Host injection contracts with each plugin.
- [ ] Update peer ranges to the narrowest rc.8-compatible versions proven by installation.
- [ ] Run each plugin's focused tests/check, build, package verification, and pack command.
- [ ] Do not expand compatibility beyond versions actually tested.

### Task 4: Prove isolated and combined stock-DSH installation

**Files:**
- Create temporarily: isolated `DSH_HOME` directories and tarballs
- Do not modify: `<user-home>/deepseek-harness` product source

- [ ] Install `@deepseek-ai/dsh@0.1.0-rc.8` into an isolated profile.
- [ ] Add packed tarballs one at a time for execution fold, model retry, session history, Codex shared pool, and core workbench.
- [ ] For every plugin, dump composed config, boot Web, assert expected Host/browser registration, and stop cleanly.
- [ ] Install all owned plugins together and repeat config and browser smoke checks to detect ID, slot, route, or CSS conflicts.
- [ ] Clearly distinguish pack-format success from real stock-DSH installation success.

### Task 5: Clean recoverable local residue

**Files:**
- Clean: untracked generated `.js`, `.d.ts`, and source-map files under `<user-home>/deepseek-harness/.worktrees/plugin-finalization-integration`
- Preserve: tracked source, commits, plans, credentials, sessions, and user data

- [ ] Verify every cleanup target is untracked generated output.
- [ ] Move targets to a timestamped Trash backup while preserving relative paths.
- [ ] Re-run `git status` and confirm only intentional non-generated files remain untracked.
- [ ] Leave historical source commits intact and inactive.

### Task 6: Migrate and restart the real profile

**Files:**
- Back up: `<user-home>/.dsh/profiles/web`
- Modify: `<user-home>/.dsh/profiles/web/package.json`
- Modify: `<user-home>/.dsh/profiles/web/cordis.patch.yml`

- [ ] Stop the existing rc.7 Web process.
- [ ] Back up the real profile before dependency mutation.
- [ ] Replace the deleted `/tmp` Codex pool tarball reference and undeclared workbench symlink with stable packed plugin installations.
- [ ] Install the validated rc.8-compatible tarballs and retain unrelated third-party plugins.
- [ ] Start stock rc.8 on `127.0.0.1:3080` and verify the default conversation, folded conversation, retry settings, history surface, shared pool degradation, and workbench navigation.

### Task 7: Final evidence and handoff

- [ ] Run final focused tests, builds, package verification, and `git diff --check` where Git exists.
- [ ] Report the exact DSH dist-tag situation (`latest` versus newer `next`).
- [ ] Report plugin-by-plugin compatibility, commands actually run, cleanup backup locations, unverified risks, and exact Git state.
- [ ] Do not commit, push, publish, merge, or rewrite history unless separately authorized for this cleanup task.
