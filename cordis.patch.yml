# Public composition boundary for the standalone Codex bundle. Installing the
# plugin only mounts it; user model and search-provider defaults are preserved.
- insert:
    - id: codex-shared-pool
      name: dsh-codex-shared-pool
