- id: codex-shared-pool
  name: dsh-codex-shared-pool
  config:
    team:
      enabled: true
      storage: memory
      bootstrapTokenRef: DSH_CODEX_SHARED_POOL_BOOTSTRAP_TOKEN
    teamClient:
      enabled: true
      baseUrl: http://127.0.0.1:3099/plugins/dsh-codex-shared-pool/team
